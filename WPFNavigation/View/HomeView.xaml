<UserControl x:Class="WPFNavigation.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFNavigation.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White"
             xmlns:viewmodels="clr-namespace:WPFNavigation.ViewModel"
             xmlns:views="clr-namespace:WPFNavigation.View">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Content="Go to First View" Height="50" Width="100" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,200,0,0" Command="{Binding NavigateToFirstViewCommand}"/>
        <Button Content="Go to Second View" Height="50" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="474,200,0,0" Command="{Binding NavigateToSecondViewCommand}"/>

        <Label Content="Home Page" FontSize="30" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="314,20,0,0"/>


    </Grid>
</UserControl>

